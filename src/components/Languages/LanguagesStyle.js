import styled from "styled-components";

export const LanguagesStyle = styled.div`
  display: flex;
  align-items: center;

  .custom-select * {
    color: rgba(0, 0, 0, 0.8);
  }

  .custom-select svg {
    fill: rgba(0, 0, 0, 0.3);
  }
`;
